import { Default } from "./variants/Default";
import type { PrivacyProps } from "./types";

/**
 * Роутер страницы политики конфиденциальности.
 *
 * Вариант пока один — роутер тут ради единообразия, как у хедера, футера
 * и 404: второй дизайн документа (например, с оглавлением сбоку)
 * добавляется тем же алгоритмом, что и второй дизайн любой секции.
 * Своего поля variant у страницы нет; когда появится второй вариант,
 * ручку нужно будет завести в конфиге и читать её здесь.
 *
 * Текст политики живёт в parts/legalBlocks.ts — он одинаков для любого
 * оформления. Как добавить новый дизайн — docs/section-system.md, раздел 7.
 */
export function Privacy(props: PrivacyProps) {
  return <Default {...props} />;
}

export default Privacy;
